# ============================================================================
# .gitignore — PharmaPM Pro
# ============================================================================

# === Node / npm (used only by tools/run_tests.js) =========================
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json
yarn.lock
.npm
.pnpm-debug.log*

# === Editors / IDEs =======================================================
.vscode/
.idea/
*.swp
*.swo
*.sublime-project
*.sublime-workspace
.history/

# === Operating system noise ===============================================
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
Desktop.ini

# === Environment / secrets ================================================
.env
.env.local
.env.*.local
*.pem
*.key

# === Build / dist outputs =================================================
dist/
build/
.cache/
.parcel-cache/

# === v2 — Next.js build artefacts (v2/.gitignore handles v2/out/) =========
v2/.next/
v2/out/

# === Test artefacts =======================================================
coverage/
*.lcov
.nyc_output/

# === Local zips & scratch =================================================
*.zip
*.tgz
*.tar.gz
/tmp/
/scratch/

# === Logs =================================================================
logs/
*.log

# === Browser dev tools ====================================================
*.map
